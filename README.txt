1. dependency

  a) anaconda3 (python2 is not supported at present)
  b) edlib
  c) pysam

2. Instlattion

  a) anaconda3 has to be installed anyway.

  Once you install anaconda, conda package manager is recommended.
  b) conda install edlib
     conda install python-edlib
  c) conda install pysam
